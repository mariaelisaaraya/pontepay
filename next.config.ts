import type { NextConfig } from "next";

// ELI: add NEXT_PUBLIC_PRIVY_APP_ID to .env.local and Vercel env.
// Get it at dashboard.privy.io → your app → Settings → App ID.
// NEXT_PUBLIC_PLATFORM_ADDRESS is the PeerlyPay Stellar escrow owner / dispute resolver address.
const nextConfig: NextConfig = {};

export default nextConfig;
